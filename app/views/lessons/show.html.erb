<div class="container">
  <br>
  <%= link_to "< Volver al curso", course_path(@course) %> <br><br>
  <h1><%= @lesson.title %></h1>
  <p><%= @lesson.description %></p>
  <div class="video">
    <%= cl_video_tag @lesson.asset.key, controls: true, controlsList: "nodownload" , height: "75%"%>
  </div>
  <br>
  <% if current_user.admin %>
    <%= link_to "Editar >", edit_lesson_path(@lesson) %> |
    <%= link_to "Eliminar >", lesson_path(@lesson), data: {
      turbo_method: :delete,
                        turbo_confirm: "Are you sure you want to delete this lesson?" } %>
  <% end %>
</div>
